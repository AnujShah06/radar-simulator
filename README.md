# Radar Signal Processing Simulator

A real-time radar simulation system that demonstrates signal processing, target detection, and tracking capabilities.

## Setup
```bash
pip install -r requirements.txt
streamlit run src/main.py
